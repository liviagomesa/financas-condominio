spring:
  application:
    name: financas-backend
  datasource:
    url: jdbc:postgresql://localhost:5434/financas
    username: financas
    password: financas
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    locations: classpath:db/migration

server:
  port: 8082

ADMIN_USERNAME: sindica
ADMIN_PASSWORD: senha-de-teste
JWT_SECRET: a-test-secret-with-at-least-32-characters
